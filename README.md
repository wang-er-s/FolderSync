# Folder Sync - 文件夹同步工具

一个基于 PySide6 的文件夹同步应用程序，支持实时监控和手动同步。

## 功能特性

- 📁 文件夹同步：支持源文件夹到目标文件夹的同步
- 👁️ 实时监控：使用 watchdog 监控文件变化
- 🖥️ 图形界面：基于 PySide6 的现代化 GUI
- 📋 同步日志：详细的操作日志记录
- ⚙️ 灵活配置：支持多种同步模式和过滤规则

## 安装

### 使用 uv（推荐）

```bash
# 安装依赖
uv sync

# 安装开发依赖
uv sync --dev
```

### 使用 pip

```bash
pip install -e .
```

## 开发

### 运行测试

```bash
uv run pytest
```

### 类型检查

```bash
uv run mypy src
```

### 运行应用

```bash
uv run folder-sync
```

## 项目结构

```
folder-sync/
├── src/
│   ├── __init__.py          # 包初始化
│   ├── __main__.py          # 应用入口
│   └── logging_config.py    # 日志配置
├── tests/
│   ├── __init__.py
│   ├── conftest.py              # pytest 配置
│   └── test_logging_config.py   # 日志测试
├── pyproject.toml               # 项目配置
├── .python-version              # Python 版本
└── README.md                    # 项目说明
```

## 依赖

- Python >= 3.13
- PySide6 >= 6.10.1
- watchdog >= 6.0.0

### 开发依赖

- pytest >= 9.0.2
- hypothesis >= 6.150.0
- pytest-qt >= 4.5.0
- mypy >= 1.19.1

## 许可证

MIT License
